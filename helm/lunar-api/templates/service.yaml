apiVersion: v1
kind: Service
metadata:
  name: {{ include "lunar-api.fullname" . }}
  labels:
    {{- include "lunar-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lunar-api.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lunar-api.fullname" . }}-test-connection"
  labels:
    {{- include "lunar-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lunar-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
